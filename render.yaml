services:
  - type: web
      name: smartorder-restaurant
          runtime: docker
              dockerfilePath: ./Dockerfile
                  plan: free
                      healthCheckPath: /
                          envVars:
                                - key: PORT
                                        value: "10000"
